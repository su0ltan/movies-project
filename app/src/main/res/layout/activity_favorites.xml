<?xml version="1.0" encoding="utf-8"?>
<?xml version="1.0" encoding="utf-8"?>
<!-- SDK XML Version 3 Compatible -->
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent">

    <!-- Title Bar -->
    <LinearLayout
        android:id="@+id/Favorites_titleBar"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:background="#6200EE"
        android:padding="8dp">

        <!-- App Title -->
        <Button
            android:id="@+id/Favorites_buttonBack"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="< Back"
            android:textColor="#FFFFFF"
            android:background="#6200EE" />
        <!-- Spacer -->
        <View
            android:layout_width="0dp"
            android:layout_height="0dp"
            android:layout_weight="1" />

        <TextView
            android:id="@+id/Favorites_Title"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Favorites List"
            android:textColor="#FFFFFF"
            android:textSize="20sp"
            android:textStyle="bold" />

        <View
            android:layout_width="0dp"
            android:layout_height="0dp"
            android:layout_weight="1" />

        <!-- Menu Button -->
        <Button
            android:id="@+id/Favorites_buttonMenu"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Menu"
            android:textColor="#FFFFFF"
            android:background="#6200EE" />
    </LinearLayout>

    <!-- Search Bar -->
    <EditText
        android:id="@+id/Favorites_editTextSearch"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:hint="Search movies..."
        android:padding="8dp"
        android:layout_below="@id/Favorites_titleBar"
        android:background="@android:drawable/edit_text" />

    <!-- ListView for Movies -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/Favorites_recyclerViewMovies"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@id/Favorites_editTextSearch"
        android:padding="8dp" />

</RelativeLayout>